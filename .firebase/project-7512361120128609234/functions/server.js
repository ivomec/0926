const { onRequest } = require('firebase-functions/v2/https');
  const server = import('firebase-frameworks');
  exports.ssrproject7512361120128 = onRequest({"id":"chart0926"}, (req, res) => server.then(it => it.handle(req, res)));
  